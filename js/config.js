/**
 * CONFIGURATION
 * 
 * For Mobile APK: 
 * Set API_BASE to your PC's local IP (e.g., 192.168.x.x)
 * Ensure both phone and PC are on the same Wi-Fi.
 */
window.APP_CONFIG = {
    API_BASE: "http://10.190.217.174:5002/api"
};
